{
  "name": "jquerydom",
  "version": "0.1.0",
  "description": "jquery template with jsdom and github page refreshed with travis",
  "main": "index.js",
  "scripts": {
    "dev": "lite-server",
    "test": "ava --verbose"
  },
  "dependencies": {
    "jquery": "*"
  },
  "devDependencies": {
    "jsdom" : "*", 
    "ava": "*",
    "lite-server": "*"
  }
}
